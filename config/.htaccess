# Folder ini hanya untuk dipanggil dari sisi server (include).
# Blokir SEMUA akses langsung dari browser/curl.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
